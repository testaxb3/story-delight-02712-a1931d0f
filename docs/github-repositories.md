# Connected GitHub Repositories

No GitHub remotes are configured for this project. The local Git configuration (`.git/config`) only contains repository settings and no `[remote]` entries, so there are currently no connected GitHub repositories or associated permissions.
